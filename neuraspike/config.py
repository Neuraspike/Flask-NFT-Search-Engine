import os

# specify the path to the where the extracted features are been stored
PATH_TO_INDEX = os.path.sep.join(['output', "features.csv"])

# initialize the path to where our output will be stored
BASE_STATIC_PATH = "static"

# initialize the path to where the uploaded images will be stored
PATH_TO_IMAGE = "img"

# initialize the path to the query images
PATH_TO_UPLOAD_DIR = "upload"